/**
 * Places the grid row and column labels on the overlay.
 * Currently only positive labels are supported.
 *
 * @param {Object} config
 * @param {Object} bounds
 */
export function drawGridNumbers(config: Object, bounds: Object): void;
export type GridLabelPositions = {
    contentTop: number;
    contentLeft: number;
};
